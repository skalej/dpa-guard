apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: minio-creds
  namespace: dpa
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: minio-creds
    creationPolicy: Owner
  data:
    - secretKey: rootUser
      remoteRef:
        key: dpa/minio
        property: rootUser
    - secretKey: rootPassword
      remoteRef:
        key: dpa/minio
        property: rootPassword